# Notebook Directory

This project is maintained as a notebook-free workflow.

- Use `docs/api-cookbook.md` for reproducible CLI/API examples.
- Keep analysis and automation in scripts under `backend/scripts/` and `scripts/`.
